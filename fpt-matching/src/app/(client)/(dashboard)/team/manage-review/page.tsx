'use client'
import {useEffect} from 'react';

const Page = () => {
    useEffect(() => {
        // const fetchData = async () => {
        //     // const response = await reviewService.getReviewByProjectId({projectId});
        //     const fectchProject = projectService.fetchById()
        // }
    }, []);
    return (
        <div>
          Mange review page
        </div>
    );
};

export default Page;